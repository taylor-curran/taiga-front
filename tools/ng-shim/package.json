{
  "name": "ng",
  "version": "0.0.0",
  "private": true,
  "description": "CLI shim so `npx ng build` runs the Taiga reference Gulp production build (this repo is AngularJS, not Angular CLI).",
  "bin": {
    "ng": "bin/ng.js"
  }
}
